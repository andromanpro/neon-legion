# Developer Deliverable

The documentation sample should include the run state, the manifest used,
the role configuration used, and all role outputs. The files are intentionally
small so a reader can understand the orchestrator contract without needing a
real model invocation.

No runtime tracker data, prompts/private content, local account names, or
machine-specific absolute paths belong in the published sample directory.
