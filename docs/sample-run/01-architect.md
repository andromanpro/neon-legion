# Architect Deliverable

This sample run demonstrates the minimal documentation flow for a demo trace.
The run should show three role handoffs, a schema-versioned state file, and
markdown deliverables that can be copied into public docs after sanitization.

Acceptance focuses on readability: keep the state file intact, include the
manifest and roles used for the run, and avoid machine-specific paths in the
published copy.
